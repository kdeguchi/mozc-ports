--- src/MODULE.bazel
+++ src/MODULE.bazel
@@ -127,6 +127,13 @@ pkg_config_repository(
     ],
 )
 
+# uim
+pkg_config_repository(
+    name = "uim",
+    exec_os = ["linux","freebsd"],
+    packages = ["uim"],
+)
+
 # Qt for Linux
 pkg_config_repository(
     name = "qt_linux",
